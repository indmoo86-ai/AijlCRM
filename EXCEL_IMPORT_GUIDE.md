# 线索Excel导入功能使用指南

## 功能概述

Excel导入功能支持批量导入线索数据，具有以下特性：

### ✅ 已实现的功能

1. **B方案导入** - 仅导入新数据
   - 按手机号自动去重
   - 跳过已存在的线索
   - 一般最后的几条才是新增数据

2. **智能状态判断**
   - 有跟踪记录 → 标记为"优先跟进"（状态2）
   - 无跟踪记录 → 标记为"持续跟进"（状态1）
   - 新增数据中没有"已签约"状态

3. **渠道识别**
   - 支持：抖音、视频号、小红书
   - 自动识别渠道关键词
   - 规范化渠道代码

4. **自动分配**
   - 新媒体线索（抖音/视频号/小红书）自动分配给姜文颖
   - 其他渠道分配给导入人

5. **客户关联**
   - 自动按手机号+省份匹配已有客户
   - 未找到则自动创建新客户

## Excel文件格式

### 列顺序（共13列）

| 列号 | 列名 | 必填 | 说明 | 示例 |
|------|------|------|------|------|
| 1 | 手机号 | ✓ | 联系电话，用于去重 | 13800138000 |
| 2 | 客户名称 | ✓ | 客户联系人姓名 | 张三 |
| 3 | 酒店名称 | | 酒店全称，默认使用客户名称 | 如家酒店北京店 |
| 4 | 省份 | | 所在省份 | 北京市 |
| 5 | 城市 | | 所在城市 | 北京市 |
| 6 | 区县 | | 所在区县 | 朝阳区 |
| 7 | 地址 | | 详细地址 | 朝阳路123号 |
| 8 | 房间数 | | 酒店房间数量 | 100 |
| 9 | 渠道来源 | | 抖音/视频号/小红书/其他 | 抖音 |
| 10 | 需求描述 | | 客户首次需求说明 | 咨询无人酒店方案 |
| 11 | 微信 | | 微信号 | wechat123 |
| 12 | 意向程度 | | 1-3的数字，1=低、2=中、3=高 | 2 |
| 13 | 跟踪记录 | | 有内容=优先跟进，空=持续跟进 | 已电话沟通，约定下周见面 |

### Excel模板示例

```
手机号          客户名称    酒店名称        省份    城市    区县    地址            房间数  渠道来源    需求描述            微信        意向程度    跟踪记录
13800138001     张三        如家北京店      北京市  北京市  朝阳区  朝阳路123号     100     抖音        咨询无人酒店方案    wechat001   2           已电话沟通
13800138002     李四        锦江上海店      上海市  上海市  浦东区  浦东路456号     80      视频号      需要智能客控        wechat002   3
13800138003     王五        汉庭深圳店      广东省  深圳市  南山区  南山路789号     120     小红书      了解PMS系统         wechat003   1           约定本周四面谈
```

## 导入流程

### 1. 准备Excel文件
- 确保文件格式为 `.xlsx` 或 `.xls`
- 文件大小不超过10MB
- 第一行为标题行（会被跳过）
- 数据从第二行开始

### 2. 打开导入功能
1. 登录艾居来CRM系统
2. 进入"线索管理"页面
3. 点击"Excel导入"按钮

### 3. 上传文件
- 拖拽文件到上传区域，或点击选择文件
- 系统会显示导入说明

### 4. 开始导入
- 点击"开始导入"按钮
- 等待导入进度条完成
- 查看导入结果

### 5. 查看结果
系统会显示：
- 导入总数
- 成功数量
- 跳过数量（已存在）
- 失败数量

## 业务规则

### 去重规则
- 按手机号进行去重
- 如果手机号已存在，则跳过该条记录
- 从Excel末尾向前处理（新数据通常在最后）

### 状态判断
```javascript
if (跟踪记录列有内容) {
  状态 = "优先跟进" (status=2)
  创建跟进记录
} else {
  状态 = "持续跟进" (status=1)
  创建导入记录
}
```

### 渠道识别
```javascript
// 自动识别并规范化
"抖音" / "douyin" → douyin
"视频号" / "weixin_video" → weixin_video
"小红书" / "xiaohongshu" → xiaohongshu
其他 → other
```

### 负责人分配
```javascript
if (渠道 in ['douyin', 'weixin_video', 'xiaohongshu']) {
  新媒体负责人 = 姜文颖
  销售负责人 = 姜文颖
} else {
  销售负责人 = 当前登录用户
}
```

### 客户关联
```javascript
客户 = 查找(手机号 AND 省份)
if (客户存在) {
  关联到已有客户
} else {
  创建新客户
  关联到新客户
}
```

## 常见问题

### Q1: 为什么有些记录被跳过？
**A**: 系统按手机号去重，如果该手机号已存在于系统中，则会跳过该条记录。

### Q2: 如何判断一条线索是"优先跟进"还是"持续跟进"？
**A**:
- 如果Excel中"跟踪记录"列有内容，则标记为"优先跟进"
- 如果为空，则标记为"持续跟进"

### Q3: 新媒体线索会自动分配给谁？
**A**: 来自抖音、视频号、小红书的线索会自动分配给姜文颖。

### Q4: 如果找不到姜文颖用户怎么办？
**A**: 系统会将线索分配给当前登录用户，并在日志中记录警告。

### Q5: 导入失败的记录会怎样？
**A**:
- 失败的记录会在结果中显示
- 可以查看详细的失败原因
- 失败的记录不会被导入系统

### Q6: 可以重复导入同一个Excel吗？
**A**: 可以，但已存在的记录（相同手机号）会被跳过。

### Q7: Excel列的顺序可以调整吗？
**A**: 不可以，必须严格按照模板的列顺序。

### Q8: 意向程度如何填写？
**A**: 填写1-3的数字：
- 1 = 低意向
- 2 = 中意向
- 3 = 高意向

## 导入示例

### 示例1：新媒体线索（抖音）
```
13900001111,陈经理,维也纳酒店,广东省,广州市,天河区,天河路1号,150,抖音,咨询智能门锁,wx001,3,已通过抖音私信初步沟通
```
**结果**：
- 状态：优先跟进
- 负责人：姜文颖
- 创建跟进记录："已通过抖音私信初步沟通"

### 示例2：其他渠道线索
```
13900002222,赵总,7天连锁,浙江省,杭州市,西湖区,西湖路2号,80,电话咨询,了解自助入住系统,wx002,2,
```
**结果**：
- 状态：持续跟进
- 负责人：当前登录用户
- 创建导入记录

## 技术实现

### 后端API
- **路径**: `POST /api/leads/import`
- **权限**: `lead:create`
- **请求类型**: `multipart/form-data`
- **文件字段**: `file`

### 前端组件
- **页面**: `/views/leads/Index.vue`
- **API**: `/api/leads.js`
- **上传组件**: Element Plus Upload

### 数据流程
```
Excel文件
  ↓ 上传
前端 (FormData)
  ↓ HTTP请求
后端API (/api/leads/import)
  ↓ 解析Excel (ExcelJS)
后端Controller (importLeadsFromExcel)
  ↓ 逐行处理
数据验证 → 去重检查 → 渠道识别 → 负责人分配
  ↓ 创建记录
数据库 (Lead + Customer + FollowUp)
  ↓ 返回结果
前端显示导入结果
```

## 更新日志

### v1.0.0 (2026-01-06)
- ✅ 实现B方案导入（按手机号去重）
- ✅ 实现智能状态判断（有跟踪记录=优先跟进）
- ✅ 支持抖音、视频号、小红书渠道
- ✅ 新媒体线索自动分配给姜文颖
- ✅ 自动创建/关联客户
- ✅ 创建跟进记录
- ✅ 前端导入界面
- ✅ 导入进度显示
- ✅ 导入结果统计

## 联系支持

如有问题，请联系技术支持或查看[系统文档](./README.md)。
