# 前端实施与集成计划

**最后更新**: 2026-01-11
**状态**: 进行中

## 一、当前状态评估

### 1.1 已完成模块
经过代码审查和构建验证，以下模块的前端页面结构和逻辑已基本实现：
- ✅ **基础框架**: Layout, Router, Store (Pinia), Axios封装
- ✅ **认证模块**: Login
- ✅ **工作台**: Dashboard (统计卡片, 图表)
- ✅ **业务模块**:
  - 线索 (Leads)
  - 客户 (Customers)
  - 产品 (Products)
  - 报价 (Quotations)
  - 合同 (Contracts)
  - 发货 (Shipments)
  - 收款 (Payments)
  - 发票 (Invoices)
  - 售后 (Services/Tickets)
  - 任务 (Tasks)

### 1.2 构建状态
- ✅ `npm run build` 成功通过
- ⚠️ 存在部分 Chunk 体积过大警告 (Index.js > 500kB)，后续需优化

## 二、剩余工作计划

虽然代码已存在，但需要进行详细的"逻辑审计"以确保与后端完美对接。

### 阶段 1: API 集成审计 (预计 1-2 天)
**目标**: 确保前端发送的参数和处理的响应与后端 API 完全匹配。

| 模块 | 检查重点 | 状态 |
|------|---------|------|
| **通用** | 响应拦截器是否统一处理了后端标准响应格式 `{code, data, msg}` | 待检查 |
| **通用** | 驼峰/下划线参数转换是否在 API 层或组件层正确处理 | 待检查 |
| **线索** | `lead_source`, `status` 枚举值是否与后端同步 | 待检查 |
| **客户** | 客户创建/编辑字段映射 | 待检查 |
| **报价** | 产品明细 (`items`) 的数据结构是否匹配后端要求 | 待检查 |
| **合同** | 金额计算和状态流转 (`confirm`, `sign`) API 调用 | 待检查 |

### 阶段 2: 功能逻辑验证 (预计 2-3 天)
**目标**: 验证复杂的业务交互逻辑。

1. **报价单创建流程**:
   - 从线索跳转到报价单新建页的参数传递 (`query` 参数解析)
   - 产品选择和金额计算逻辑 (前端计算 vs 后端计算)

2. **合同执行流程**:
   - 合同生成时的字段回填
   - 发货/收款进度更新的前端展示逻辑

3. **关联数据加载**:
   - 下拉列表 (客户、产品、合同) 的分页加载或全量加载策略

### 阶段 3: UI/UX 优化 (预计 1-2 天)
1. **加载状态**: 确保所有异步操作都有 `v-loading` 或按钮 loading 状态
2. **错误处理**: 表单验证错误和 API 请求错误的友好提示
3. **响应式适配**: 检查关键表格在小屏幕下的显示

## 三、执行策略

我们将采用 **"模块逐个击破"** 的策略，按照业务依赖顺序进行审计和修复：

1. **基础配置层**: Axios 拦截器, 路由守卫
2. **基础数据层**: 产品, 客户
3. **核心业务流**: 线索 -> 报价 -> 合同
4. **履约流程**: 发货 -> 收款 -> 发票 -> 售后

## 四、下一步行动

立即开始 **阶段 1: API 集成审计**，首先检查 Axios 封装和基础响应处理。
